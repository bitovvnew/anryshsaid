<UserControl x:Class="SAIL.Views.MinesweeperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel>
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="💣 Сапёр SAIL" FontFamily="{StaticResource AppFont}"
                           FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Пройди сапёра. Проиграл — мем «ЭТО НЕ ВЫХОД» на 5 сек · iOS 26 vibes"
                           FontFamily="{StaticResource AppFont}" FontSize="13"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0"/>
            </StackPanel>

            <Border Style="{StaticResource GlassCard}" HorizontalAlignment="Left">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#22FF375F" CornerRadius="10" Padding="12,8" Margin="0,0,8,0">
                                <TextBlock x:Name="MineCounter" Text="💣 15" FontFamily="{StaticResource AppFont}"
                                           FontSize="14" Foreground="{StaticResource AccentPinkBrush}"/>
                            </Border>
                            <Border Background="#2230D158" CornerRadius="10" Padding="12,8">
                                <TextBlock x:Name="TimerText" Text="⏱ 0:00" FontFamily="{StaticResource AppFont}"
                                           FontSize="14" Foreground="{StaticResource AccentGreenBrush}"/>
                            </Border>
                        </StackPanel>
                        <Button Grid.Column="1" Content="🔄 Заново" Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,0" Click="Restart_Click"/>
                        <Button Grid.Column="2" Content="😇 Лёгкий" Style="{StaticResource SecondaryButton}"
                                Click="EasyMode_Click"/>
                    </Grid>

                    <UniformGrid x:Name="BoardGrid" Rows="10" Columns="10" Width="360" Height="360"/>

                    <TextBlock x:Name="StatusText" Text="ЛКМ — открыть · ПКМ — флаг · Не наступи на 15%!"
                               FontFamily="{StaticResource AppFont}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
